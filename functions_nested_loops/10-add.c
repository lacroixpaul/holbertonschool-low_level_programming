#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result.
 * @a: premier entier.
 * @b: second entier.
 * Return: results
 */

int add(int a, int b)
{
return (a + b);
}
