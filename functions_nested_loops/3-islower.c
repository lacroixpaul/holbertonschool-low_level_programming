#include "main.h"
#include <stdio.h>

/**
 * _islower  checks for lowercase character.
 * Returns 1 if c is lowercase, returns 0 otherwise.
 **/

int _islower(int c)
{
	if (c > 96 && c < 123)
	{
	return (1);
	}
	else
	{
	return (0);
}
}
